export type UserInfo = {
      firstName: string;
      lastName: string;
      email: string;
      password: string;
}

export type ProductInfo = {
      name: string;
      size: string;
      color: string;
}